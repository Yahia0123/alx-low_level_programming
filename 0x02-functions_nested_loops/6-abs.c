#include "main.h"
#include <stdio.h>

/**
 * _abs - function that computes the absolute value of an integer
 *
 * Return: Always 0 (Success)
*/

int _abs(int)
{
	if (n < 0)
		n = (-1) * n;
	return (n);
}
