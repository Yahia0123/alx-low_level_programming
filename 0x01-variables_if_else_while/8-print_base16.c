#include <stdio.h>

/**
 * main - entry point
 *
 * Description:prints all the numbers of base 16 in lowercase
 *
 * Return: 0 (Success)
*/

int main(void)
{
	int X = 1;

	while (X < 16)
	{
	if (X >= 10)
		putchar(a);
		a++;
	else
		putchar(X + '0');
	X++;
	}
	putchar('\n');
	return (0);
}
