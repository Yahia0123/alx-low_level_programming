#include<stdio>

/**
 * main : Entry point
 *
 * Description: the positive or negative.
 *
 * return: 0
*/

int main(void)
{
	int n;
	if(n > 0){printf("%d is positive\n", n)};
	if else(n == 0){printf("%d is zero\n", n)};
	else{printf("%d is negative\n", n)};
	return (0);
}
